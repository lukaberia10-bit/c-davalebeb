namespace homework4
{
    internal class Program
    {
        static void Main(string[] args)
        {
            //int[] array1 = { 1, 2, 3 };
            //int[] array2 = { 4, 5, 6 };

            //int[] resultArray = new int[array1.Length + array2.Length];

            //Array.Copy(array1, 0, resultArray, 0, array1.Length);
            //Array.Copy(array2, 0, resultArray, array1.Length, array2.Length);

            //Console.Write("ResultArray = [");
            //for (int i = 0; i < resultArray.Length; i++)
            //{
            //    Console.Write(resultArray[i]);
            //    if (i < resultArray.Length - 1) Console.Write(", ");
            //}
            //Console.WriteLine("]");


            //int[] array = { 3, 5, -4, 8, 11, 1, -1, 6 };

            //Console.Write("Enter Target TargetSum: ");
            //int.TryParse(Console.ReadLine(), out int targetSum);

            //List<int[]> resultList = new List<int[]>();

            //for (int i = 0; i < array.Length; i++)
            //{
            //    for (int j = i + 1; j < array.Length; j++)
            //    {
            //        if (array[i] + array[j] == targetSum)
            //        {
            //            resultList.Add(new int[] { array[i], array[j] });
            //        }
            //    }
            //}

            //Console.Write("resultArray = [");
            //for (int i = 0; i < resultList.Count; i++)
            //{
            //    Console.Write($"[{resultList[i][0]}, {resultList[i][1]}]");
            //    if (i < resultList.Count - 1) Console.Write(", ");
            //}
            //Console.WriteLine("]");
        }
    }
}
